# Olá, Mundo

Na rota `/`, a estrutura a ser renderizada será:

```
<PaginaPadrao>
    <Inicio />
</PaginaPadrao>
```

Já na rota `/sobre` a estrutura a ser rederizada será:

```
<PaginaPadrao>
    <Sobre />
</PaginaPadrao>
```

